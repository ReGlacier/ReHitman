---
description: Resume autonomous ScriptInterfaces reversing from the durable checkpoint
agent: script-interface-reverser
subtask: true
---
Run as an isolated subtask. Load the `script-interface-reversing` skill and resume from the durable checkpoint. At the beginning, inspect the PC/iOS/PS2 IDA instances with the hyper MCP tools and use the xexe MCP library to obtain relevant function, type, global, and address information before resolving signatures. For every meaningful entry, rename the corresponding function in the PC IDA database through a `hyper_*` MCP call in the same batch, verify the resulting name, and persist the terminal naming result before moving the cursor. PC names must be demangled readable method names; never copy mangled iOS or ABI-decorated names into the PC database. Never rename verified empty stubs; leave an address comment where supported and persist that outcome. Also complete any outstanding PC naming backfill.

When reconciling signatures, do not blindly preserve a target-platform `char` when the value is semantically a `bool`: inspect control flow, callers, field usage, and xexe type information, and keep `bool` whenever the combined evidence establishes boolean meaning. Name arguments with Hungarian notation at minimum: `i` for integers, `b` for booleans, and `p` for pointers (for example `iCount`, `bEnabled`, `pEntity`); replace generic decompiler argument names whenever their types are known. A rename is not complete until the PC hyper/IDA database confirms the non-mangled name. Continue batch by batch until the complete table satisfies every completion criterion or a genuinely ambiguous, fully investigated blocker requires one focused user decision. Do not stop after a single batch. Return a concise summary only after the durable checkpoint has been written.
