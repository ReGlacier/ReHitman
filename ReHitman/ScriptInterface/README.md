# Glacier Script Interface

This directory contains Glacier's game specific script interface methods & script interface table. It is used for
propagate methods & in-game api to scripting dlls. This static library depends on Glacier & BloodMoney targets.