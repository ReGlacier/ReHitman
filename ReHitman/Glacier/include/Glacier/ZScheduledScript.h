#pragma once

#include <Glacier/ZScheduledEvent.h>

namespace Glacier {
    class ZScheduledScript : public ZScheduledEvent {
    public:
        //vftable (no changes)
        //data (no changes)
    };
}